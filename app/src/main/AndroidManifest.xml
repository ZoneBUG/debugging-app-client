<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zonebug.debugging">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Debugging">
        <activity
            android:name="com.zonebug.debugging.activity.setting.MyPageActivity"
            android:exported="true" />
        <activity
            android:name="com.zonebug.debugging.activity.register.RegisterActivity"
            android:exported="true" />
        <activity
            android:name="com.zonebug.debugging.activity.search.SearchActivity"
            android:exported="true" />
        <activity
            android:name="com.zonebug.debugging.activity.report.ReportActivity"
            android:exported="true" />
        <activity
            android:name="com.zonebug.debugging.activity.community.CommunityActivity"
            android:exported="true" />
        <activity
            android:name="com.zonebug.debugging.activity.aboutus.AboutUsActivity"
            android:exported="true" />
        <activity
            android:name="com.zonebug.debugging.activity.login.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:exported="true" />
    </application>

</manifest>